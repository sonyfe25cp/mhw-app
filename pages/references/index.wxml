<!--pages/references/index.wxml-->
<text>资料汇总</text>
<view class="links">
  <navigator url="/pages/list/list?type=recommend">
    <image src="/assets/link-01.png"/>
  </navigator>
  <navigator url="/pages/forum/forum">
    <image src="/assets/link-02.png"/>
  </navigator>
</view>

<view class="grids">
  <!-- <navigator class="item" wx:for="{{categories}}" wx:key="id" url="/pages/list/list?cat={{item.id}}"> -->
  <navigator class="item" wx:for="{{categories}}" wx:key="id" url="{{item.link || '/pages/list/list?cat={{item.id}}' }}">
    <image src="{{item.icon}}"/>
    <text>{{item.text}}</text>
  </navigator>
</view>

<contact-button 
  type="default-light" 
  size="20"
  session-from="weapp"
>
</contact-button>
